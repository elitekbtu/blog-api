from django.urls import path
# need to import the WebSocketServerProtocol to register it in the ProtocolTypeRouter in asgi.py


url_patterns = [
    # WebSocket URL patterns will be added here in the future
]
